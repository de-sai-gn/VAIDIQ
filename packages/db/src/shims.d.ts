// `react-native-url-polyfill/auto` is a side-effect-only import that ships no
// type declaration for its `/auto` subpath. Declare it so the package type-checks.
declare module "react-native-url-polyfill/auto";
